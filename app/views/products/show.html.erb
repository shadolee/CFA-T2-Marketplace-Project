<div class="registration-bg">
  <div class="container">
    <div class="row">
      <div class="col-md-4 col-md-offset-4">
        <div class="log-in panel">
          <div class="panel-heading">

<p id="notice"><%= notice %></p>

<h2>Buy USA Citizenship</h2>

<p>
  <strong>Price:</strong>
  <%= number_to_currency(@product.price) %>
</p>

<p>
  <strong>Title:</strong>
  <%= @product.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<p>
  <strong>User:</strong>
  <%#= @product.user.email  %>
</p>

<%= link_to 'Add to Cart', new_booking_path(product_id: @product.id), class: 'btn btn-primary' %>

<% @product.images.each do |image| %>
  <img src="<%= image.url %>">
<% end %>
<br><br>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>

            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
